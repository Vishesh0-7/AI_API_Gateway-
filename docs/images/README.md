Drop screenshots here with these exact filenames and they'll render in the
main README automatically:

- `dashboard-overview.png` — the full dashboard, normal/healthy state
- `chaos-controls.png` — the chaos controls section mid-demo (e.g. an
  upstream in "error" mode with its circuit OPEN)
- `ai-incident-report.png` — an AI incident report card with a
  circuit-breaker suggestion showing
