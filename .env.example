# Copy to .env (gitignored) and fill in your key. docker-compose auto-loads
# .env from this directory and substitutes ${GROQ_API_KEY} in
# docker-compose.yml -- the key is never written into any tracked file.
# Get a free key at https://console.groq.com/keys
GROQ_API_KEY=
